flask>=2.3.0
aiohttp>=3.8.0
werkzeug>=2.3.0
python-dotenv>=0.19.0
videodb  # No version specified means it will install the latest version available.
llama_index  # Specify the version if needed, or leave as-is for the latest.
llama-index-retrievers-videodb
